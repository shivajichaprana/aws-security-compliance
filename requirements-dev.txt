# Development and CI dependencies for the Lambda evaluators.
# The Lambda runtime already provides boto3/botocore; these pins make local
# runs and CI deterministic.
boto3>=1.34,<2.0
botocore>=1.34,<2.0
pytest>=8.0,<9.0
yamllint>=1.35,<2.0
